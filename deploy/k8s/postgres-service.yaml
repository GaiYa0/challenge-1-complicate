apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: challenge-demo
  labels:
    app.kubernetes.io/name: postgres
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
