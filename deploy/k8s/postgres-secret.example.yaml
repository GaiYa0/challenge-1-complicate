# 复制为 postgres-secret.yaml 并填入强密码后 kubectl apply -f postgres-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: challenge-demo
type: Opaque
stringData:
  username: "user"
  password: "CHANGE_ME"
  database: "dbname"
