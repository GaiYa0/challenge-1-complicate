# 已合并至根目录 docker-compose.yml；保留本文件以便旧脚本仍可使用 -f docker-compose.backend.yml。
# 用法：docker compose -f docker-compose.backend.yml --env-file .env.dev up -d --build
# 可选 Kafka：docker compose -f docker-compose.backend.yml --env-file .env.dev --profile kafka up -d --build

include:
  - docker-compose.yml
