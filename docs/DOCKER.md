# Docker 说明（已整合）

Docker 运行文档已统一到：

- 根目录 `README.md`（上线方式、访问地址、排错）
- `run/README.md`（一键脚本入口）
- `docs/README.md`（文档导航）

推荐命令：

```bash
./run/docker-up.sh
./run/docker-logs.sh backend
./run/docker-down.sh
```
